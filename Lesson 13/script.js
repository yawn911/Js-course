// script.js
const getFullName = (firstname, lastName) => {
  return `My fullname is ${firstname} ${lastName}`;
};

function getNumber(num) {
  return num + 5;
}

module.exports = { getNumber, getFullName };
